<div class="container-fluid wrapper">
    <div class="row-fluid">
        <div class="span12">
            <h2 style="margin-top: 95px;">Fehlerhafte Parameter</h2>
            <hr />
            <p>
                Einer oder mehrere der gegebenen Parametern entspricht nicht der erwarteten Spezifikation.
            </p>
            <ul class="errors" style="list-style: none; margin: 0;">
            {% for name, msg in error_messages %}
                <li class="error">
                    <span class="label label-important">{{ name }}</span> {{ msg }}
                </li>
            {% endfor %}
            </ul>
        </div>
    </div>
    <div class="push"></div>
</div>

